# uv 로 관리하는 프로젝트 설정입니다. WebSocket 지원은 uvicorn[standard] 에 포함됩니다.
# - 실행:  uv run uvicorn main:app --reload
[project]
name = "led-websocket"
version = "0.1.0"
description = "06 웹서버 — 예제 03: LED 제어판 WebSocket 버전 (실시간 push)"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
]
