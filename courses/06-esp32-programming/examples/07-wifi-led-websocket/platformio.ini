; 07-wifi-led-websocket — WiFi 연결 + LED 서버에 WebSocket 으로 붙기 (push 즉시 반영 + 버튼으로 전송)
[env:esp32dev]
platform = espressif32
board = esp32dev
framework = arduino
monitor_speed = 115200
; WebSocket 클라이언트 라이브러리 (arduinoWebSockets). 첫 빌드 때 자동으로 내려받습니다.
lib_deps = links2004/WebSockets@^2.6.1
